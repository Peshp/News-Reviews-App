﻿@using News_Reviews.Models.UserViewModels
@model LoginVewModel

@{
    ViewData["Title"] = "Sign in";
}

<h1 class="text-center" style="color: white">@ViewData["Title"]</h1>

<partial name="_CssStylePartial" />

<div class="register-container">
    <div class="register-form">
        <div class="row">
            <div class="col-md-12">
                <form id="registerForm" method="post">
                    <hr />
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="text-center mb-3">
                        <label asp-for="@Model.Username" class="field-label">Enter Username</label>
                        <input class="form-control" autocomplete="username" aria-required="true" asp-for="@Model.Username" />
                        <span asp-validation-for="@Model.Username" class="text-danger"></span>
                    </div>

                    <div class="text-center mb-3">
                        <label asp-for="@Model.Password" class="field-label">Enter Password</label>
                        <input class="form-control" type="password" autocomplete="password" aria-required="true" asp-for="@Model.Password" />
                        <span asp-validation-for="@Model.Password" class="text-danger"></span>
                    </div>

                    <div class="text-center">
                        <button id="loginSubmit" type="submit" class="btn btn-primary">Sign In</button>
                    </div>

                    <div class="text-center mt-3">
                        <span class="text-light">Don't have an account? </span>
                        <span class="text-light">Sign up </span><a href="@Url.Action("Register", "User")" class="text-primary">here</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
